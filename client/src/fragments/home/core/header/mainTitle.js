import styled from "styled-components";

const MainTitleStyled = styled.section`
    flex: 1.34;
`;

export const MainTitle = ({}) => {
    return (
        <MainTitleStyled>
        </MainTitleStyled>
    )
}