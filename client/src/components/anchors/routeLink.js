import styled from "styled-components";

const RouteLinkStyled = styled.nav`
    margin: 0 40px;
`

export const RouteLink = ({where})=> {
    return (
        <RouteLinkStyled ></RouteLinkStyled>
    )
}